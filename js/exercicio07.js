//Declarando variaveis 
const usuario = prompt ("Digite seu usuario")
let senha = prompt ("Digite a sua senha")

// condicionais do usuario 
        if ( usuario === "admin" && senha === "1234") {
            alert ("Acesso Permitido")
        } else {
            alert ("Acesso negado")
        }


// = é usado para atribuição, ou seja, para colocar um valor em uma variável.
// === é usado para comparação estrita, verificando se o valor e o tipo são iguais.